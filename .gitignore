
# Upscaler output — intermediates. Only the knocked-out -PRINT.png files need
# hosting for Printful to fetch; keeping both would double a repo that already
# stores every version of every file forever.
solkast-designs/
.DS_Store
